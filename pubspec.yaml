name: video_player_hook
description: A new Flutter project.

publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ">=2.16.1 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.2
  video_player: ^2.3.0
  flutter_hooks: ^0.18.2+1
  auto_route: ^3.2.4

dev_dependencies:
  flutter_test:
    sdk: flutter

  build_runner:
  auto_route_generator: ^3.2.3

  flutter_lints: ^1.0.0


flutter:
  uses-material-design: true

  assets:
    - assets/bridge.mp4
    - assets/sample-mp4-file.mp4
